/**
 * ตัวกรองที่ต้องล้างทุกครั้งที่เลือก/ยกเลิกแบรนด์ (ทั้งจาก "Shop by Brand" ที่หน้าแรก
 * และ sidebar แบรนด์ตอนกำลัง browse อยู่แล้ว)
 *
 * บั๊กที่เจอจริง: ผู้ใช้ค้นหา "AirPods" ไว้ก่อน แล้วไปกด "Huawei" ใน Shop by Brand
 * ตัวอักษรค้นหาเก่ายังติดค้างอยู่ใน URL param `q` (โค้ดเดิม spread แค่ ...prev แล้วเปลี่ยนแค่
 * brands/category) กลายเป็นกรอง "แบรนด์ Huawei" ซ้อนกับ "ชื่อสินค้ามี AirPods" พร้อมกัน
 * ไม่มีสินค้าไหนตรงทั้งสองเงื่อนไข เลยเห็น "ไม่พบสินค้า" ทั้งที่มีสินค้าจริง 100+ ชิ้น
 *
 * ช่วงราคา (min/max) และหมวดย่อย (ramSpec/ramGen/jetsonType/compType) มีความเสี่ยงแบบเดียวกัน
 * เลยล้างไปพร้อมกันทั้งหมด — ยกเว้น category ซึ่งแต่ละจุดเรียกตัดสินใจเองว่าจะล้างไหม (เช่น
 * Smart Life ต้องการคงหมวดไว้เพื่อให้ sub-filter ของหมวดนั้นยังโชว์อยู่)
 */
export const CLEAR_STALE_BROWSE_FILTERS = {
  q: "",
  min: 0,
  max: 100000,
  sub: "",
  ramSpec: "",
  ramGen: "",
  jetsonType: "",
  compType: "all",
} as const;
